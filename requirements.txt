# Core dependencies for MCP Guard Security Scanner
requests>=2.31.0
pyyaml>=6.0
asyncio-compat>=0.1.2

# Optional dependencies for enhanced functionality
websockets>=11.0.0  # For WebSocket MCP server testing
aiohttp>=3.8.0      # For HTTP-based dynamic analysis
tomli>=2.0.0        # For Python project file parsing (Python < 3.11)

# Security analysis tools (optional - will fallback gracefully if not available)
bandit>=1.7.0       # Python security analysis
safety>=2.3.0       # Python dependency vulnerability checking

# Development dependencies (optional)
pytest>=7.0.0       # Testing framework
pytest-asyncio>=0.21.0  # Async testing support
pytest-cov>=4.0.0   # Coverage reporting
black>=23.0.0       # Code formatting
flake8>=6.0.0       # Linting

# Documentation dependencies (optional)
mkdocs>=1.5.0       # Documentation generation
mkdocs-material>=9.0.0  # Material theme for docs